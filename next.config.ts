import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Product image URLs come from Open Food Facts (remote, https). We render
  // them with plain <img> tags, so no remotePatterns config is required.
};

export default nextConfig;
